package core;

/**
 * There may be other implementations in the future, so there is an interface
 * @author alp
 *
 */
public interface IWaterShedMethods
{
	 public abstract int getWater(Integer[] terrain);
}
